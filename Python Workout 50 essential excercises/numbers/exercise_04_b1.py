######################
# Hexadecimal output #
######################

# Reimplement the solution for this exercise such that it does not use the int()
# function at all, but rather the built-in ord() and chr() functions to identify
# the characters that arent legal for the number base.

def hex_to_dec():

    dec_num = 0
    hex_num = input("Choose a hex digit to convert")
    
    for index, digit in enumerate(num[::-1]):

        if 48 <= ord(digit) <= 57:
            pass
        elif 97 <= ord(digit) <= 102:
            pass

        decnum = 
